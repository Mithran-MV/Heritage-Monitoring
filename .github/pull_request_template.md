## What changed

<!-- One or two sentences. -->

## Why

<!-- The problem this solves. -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] `npm run build` succeeds
- [ ] No secrets added to tracked files
- [ ] If a threshold or metric changed, `src/lib/metrics.ts` and the README table agree
